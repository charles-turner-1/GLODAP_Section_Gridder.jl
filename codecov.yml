coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%
comment:
  layout: "header, diff, flags, files"
  require_changes: true

ignore:
  - "**/.julia/**"
  - "**/deps/**"
  - "**/test/**"
